<Window x:Class="AutoWeighbridgeSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng Nhập Hệ Thống" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#1E1E1E" CornerRadius="15" BorderBrush="#333" BorderThickness="2" Padding="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="⚖️" FontSize="50" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="TRẠM CÂN TỰ ĐỘNG" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="KHOÁNG SẢN RẠNG ĐÔNG" FontSize="16" FontWeight="Bold" Foreground="#2196F3" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Tên đăng nhập:" Foreground="LightGray" FontSize="14" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="16" Background="#2D2D2D" Foreground="White" 
                     Padding="10" BorderThickness="0" Margin="0,0,0,15" Height="40" VerticalContentAlignment="Center"/>

            <TextBlock Text="Mật khẩu:" Foreground="LightGray" FontSize="14" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" 
                         FontSize="16" Background="#2D2D2D" Foreground="White" 
                         Padding="10" BorderThickness="0" Margin="0,0,0,10" Height="40" VerticalContentAlignment="Center">
                <PasswordBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}"/>
                </PasswordBox.InputBindings>
            </PasswordBox>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="#E53935" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15" TextWrapping="Wrap" TextAlignment="Center" Height="40"/>

            <Button Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}" 
                    Content="ĐĂNG NHẬP" Height="45" FontSize="16" FontWeight="Bold" Foreground="White" Cursor="Hand" Margin="0,0,0,15">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#2196F3"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Content" Value="ĐANG KIỂM TRA..."/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Click="BtnExit_Click" Content="Thoát Phần Mềm" Background="Transparent" Foreground="Gray" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" Background="Transparent" TextDecorations="Underline" HorizontalAlignment="Center"/>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Border>
</Window>