<Window x:Class="AutoWeighbridgeSystem.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Khởi động hệ thống" Height="300" Width="500"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        Topmost="True">

    <Border Background="#1A1A1A" CornerRadius="15" BorderBrush="#333" BorderThickness="1">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo/Icon -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock Text="⚖️" FontSize="40" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="AUTOWEIGHBRIDGE SYSTEM" Foreground="White" FontSize="20" FontWeight="Bold" LetterSpacing="2"/>
                    <TextBlock Text="HỆ THỐNG TRẠM CÂN TỰ ĐỘNG THÔNG MINH" Foreground="#2196F3" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>

            <!-- Status Text -->
            <TextBlock Grid.Row="2" Text="{Binding Status}" Foreground="#AAA" FontSize="13" 
                       HorizontalAlignment="Left" Margin="5,0,0,8"/>

            <!-- Progress Bar -->
            <Grid Grid.Row="3">
                <ProgressBar Value="{Binding Progress}" Height="8" Foreground="#2196F3" Background="#333" BorderThickness="0">
                    <ProgressBar.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </ProgressBar.Resources>
                </ProgressBar>
                <TextBlock Text="{Binding Progress, StringFormat={}{0}%}" Foreground="White" FontSize="10" 
                           FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-20,5,0"/>
            </Grid>
            
            <TextBlock Grid.Row="4" Text="Loading resources..." Foreground="#444" FontSize="10" HorizontalAlignment="Right" Margin="0,5,0,0"/>
        </Grid>
    </Border>
</Window>
