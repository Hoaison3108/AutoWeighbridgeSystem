<UserControl x:Class="AutoWeighbridgeSystem.Views.NotificationHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoWeighbridgeSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Background="#121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Background="#1A1A1A" Padding="20,15" BorderBrush="#333" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔔" FontSize="24" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <StackPanel>
                        <TextBlock Text="LỊCH SỬ THÔNG BÁO" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Xem lại toàn bộ nhật ký sự kiện của hệ thống" FontSize="12" Foreground="#888"/>
                    </StackPanel>
                </StackPanel>

                <Button HorizontalAlignment="Right" 
                        Command="{Binding ClearAllHistoryCommand}"
                        Content="XOÁ TẠO BỘ LỊCH SỬ" 
                        Background="#E53935" Foreground="White" 
                        Padding="15,8" BorderThickness="0" Cursor="Hand" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <ItemsControl ItemsSource="{Binding History}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1E1E1E" CornerRadius="8" Margin="0,0,0,10" Padding="15" BorderBrush="#2A2A2A" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Time Stamp -->
                                <Border Background="#252525" CornerRadius="4" Padding="8,4" VerticalAlignment="Top" Margin="0,2,15,0">
                                    <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm:ss}" Foreground="#2196F3" FontWeight="Bold" FontSize="12"/>
                                </Border>

                                <StackPanel Grid.Column="1">
                                    <Grid>
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="{Binding Color}" FontSize="15"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat=dd/MM/yyyy}" HorizontalAlignment="Right" Foreground="#555" FontSize="11"/>
                                    </Grid>
                                    <TextBlock Text="{Binding Message}" Foreground="#AAA" TextWrapping="Wrap" FontSize="13" Margin="0,5,0,0" LineHeight="20"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State (Optional) -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" 
                    Visibility="{Binding History.Count, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
            <!-- Giả định có converter hoặc logic ẩn hiện nếu danh sách trống -->
        </StackPanel>
    </Grid>
</UserControl>
